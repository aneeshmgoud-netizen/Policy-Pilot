// Increment whenever the text representation used for precedent embeddings
// changes. Retrieval accepts only this version; older vectors require an
// explicit reindex and can never be mixed into similarity results.
export const CURRENT_PRECEDENT_EMBEDDING_VERSION = 2;
